#pragma once

#include "../common/options.h"
#include "../common/lookup.h"

const lookup_internal_name lookup_5_table[] = {
    {"500gp", "500 GP (5GP3 Ver. C)"},
};

const size_t lookup_5_count = A_SIZE(lookup_5_table);
