#pragma once

#include "../common/options.h"
#include "../common/lookup.h"

const lookup_internal_name lookup_0_table[] = {
    {"005", "005"},
};

const size_t lookup_0_count = A_SIZE(lookup_0_table);
